FROM node:22-alpine

WORKDIR /app

# Install build dependencies for canvas / native bindings if needed
RUN apk add --no-cache libc6-compat python3 make g++

COPY package.json package-lock.json ./

RUN npm ci

COPY . .

EXPOSE 3000

ENV PORT=3000
ENV HOSTNAME="0.0.0.0"

CMD ["npm", "run", "dev"]
