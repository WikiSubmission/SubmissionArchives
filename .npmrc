# Lock dependencies to exact versions to avoid drift between machines
save-exact=true

# Keep optional platform dependencies intact
include=optional

# Prevent npm from modifying package-lock on CI / cross-machine runs
legacy-peer-deps=false
